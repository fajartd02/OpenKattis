#include <stdio.h>
#include <string.h>

int main()
{
    int kasus;
    int i;
    char huruf[21];
    char cek[9];
    int x;
    int y;
    int j, k;
    int nilai = 0;
    int ans = 0;

    scanf("%d", &kasus);
    for (i = 1; i <= kasus; i++)
    {
        scanf("%s", &huruf);
        scanf("%s", &cek);
        x = strlen(huruf);
        y = strlen(cek);

    }

    return 0;
}